const constants = {
  DEFAULT_PAGE: 1,
  DEFAULT_PAGE_SIZE: 20,
  Messages: {
    VALIDATION_REQUIRED: "is a required filed",
    VALIDATION_INTEGER_ARRAY: "should be an Integer array"
  }
}

export default constants